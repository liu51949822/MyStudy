# 📖 MCP 第 4 课：数据传输方式

## Stdio（标准输入/输出）
- 适合本地进程间通信
- 通过 stdin/stdout 传输 JSON
- 启动快，延迟低

## SSE（Server-Sent Events）
- 适合远程访问
- 通过 HTTP 通信
- 支持跨网络调用

## 启动方式
```bash
# Stdio
npx tsx src/server.ts

# SSE
npx tsx src/server-sse.ts
```
